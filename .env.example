# ===================================
# REMOTE PLANKA CONFIGURATION
# ===================================
# Replace these values with your actual remote Planka instance details

# Your Remote Planka Instance URL (CHANGE THIS)
PLANKA_BASE_URL=http://localhost:3333

# Your Planka Account Credentials (CHANGE THESE)
PLANKA_AGENT_EMAIL=demo@demo.demo
PLANKA_AGENT_PASSWORD=demo

# MCP Server Configuration (runs locally)
MCP_KANBAN_PORT=3008
SERVER_PORT=3008

# ===================================
# NOTES:
# ===================================
# - Copy this file to .env and update with your actual credentials
# - This configuration connects to a REMOTE Planka instance
# - Do NOT run "npm run up" (that starts local Docker containers)
# - Run "npm run start-node" or "npm run inspector" instead
# - Make sure your remote Planka is accessible from your machine
# - Keep .env file secure and DO NOT commit credentials to git

# ===================================
# DOCKER SETTINGS (NOT USED FOR REMOTE)
# ===================================
# The following are only needed if running local Planka with Docker
# You can ignore/delete these when using remote Planka

PLANKA_PORT=3333
BASE_URL=http://localhost:3333
TRUST_PROXY=1
SECRET_KEY=secretkey
PLANKA_ADMIN_NAME=Demo User
PLANKA_ADMIN_USERNAME=demo
POSTGRES_USER=postgres
POSTGRES_PASSWORD=postgres
POSTGRES_DB=planka